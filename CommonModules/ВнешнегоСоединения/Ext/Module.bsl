Процедура ОбработкаРасходных() Экспорт
	Выборка = Документы.Поступление.Выбрать();
	Пока Выборка.Следующий() Цикл
		ДокОбъект = Выборка.ПолучитьОбъект();
	КонецЦикла;
КонецПроцедуры

Функция ОбработкаПриходных() Экспорт
	Выборка = Документы.Поступление.Выбрать();
	й = 0;
	Пока Выборка.Следующий() Цикл
		ДокОбъект = Выборка.ПолучитьОбъект();
		й = й + 1;
	КонецЦикла;
	
	Возврат й;
КонецФункции