
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	Для каждого СтрокаТЧ Из Начисления Цикл
		НоваяЗапись = Движения.Начисления.Добавить();
		НоваяЗапись.ПериодРегистрации = началоМесяца(Дата);
		НоваяЗапись.Организация = Организация;
		НоваяЗапись.Сотрудник = СтрокаТЧ.Сотрудник;
		НоваяЗапись.ВидРасчета = СтрокаТЧ.ВидРасчета;
		НоваяЗапись.Сумма = СтрокаТЧ.Сумма;
	КонецЦикла;
	
	Движения.Начисления.Записать(Истина);	
	
	Отказ = Истина;
КонецПроцедуры
